/**
 * 通用上传组件（拖拽上传）
 * - 支持 .pdf/.docx，显示文件大小限制提示（≤20MB）
 * - 上传后回调文件信息
 */
// TODO(功能阶段): 基于 AntD Upload.Dragger 实现，支持 beforeUpload 校验
//   扩展名与大小，调用 api/upload 后通过 onChange 回调 { fileId, filename }
export default function FileUpload() {
  return null;
}
