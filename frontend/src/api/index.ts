/**
 * 接口定义：按功能模块导出所有 API 调用方法
 * 与后端 API 契约（规格 5.2 节）一一对应
 */
// TODO(功能阶段): 实现以下接口
//   - upload(file): FormData → POST /upload
//   - qaAsk(fileId, question, history?): POST /qa/ask
//   - qaSummary(fileId): POST /qa/summary
//   - convertPdf(file): FormData → POST /convert/pdf2word
//   - generateDoc(title, outline): POST /generate/doc
//   - downloadUrl(fileId): 拼接 GET /download/:fileId
