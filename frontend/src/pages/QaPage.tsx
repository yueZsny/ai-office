/**
 * 问答页：上传文档 → 提问 → 显示回答与引用
 * - 支持连续提问（保留对话上下文 history）
 * - 回答内容用 Markdown 渲染（react-markdown 待加入依赖）
 */
// TODO(功能阶段): 实现上传 → 提问 → 回答展示（含 sources 引用）→ 连续问答
export default function QaPage() {
  return <div>问答页（待实现）</div>;
}
